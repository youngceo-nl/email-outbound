we're going to make this software a lead generator primarily. 

the apify API key is in `.env.local` as `APIFY_TOKEN` (has ~25 dollars of credit on it).

we're going to do the following scrapes on highly qualified accounts that are likely to contain people in our ICP. 

https://www.instagram.com/pierree/
this is an example of a high yield seed account

So this software is going to handle lead generation and verification through backfill and then AI.

If you have any questions, ask them. make a subfolder in this folder called overhaul, you're going to put this md in it and all mds related to this part of the project.