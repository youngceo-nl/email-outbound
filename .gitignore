node_modules
.next
.next.corrupted-*
.env*
!.env.local.example
!.env.production.example
.DS_Store
*.log
.vercel
.turbo
.env*.local
# Plaintext shared-credentials handoff — must never be committed.
CREDENTIALS.local.md

# Voice biometric data - never commit
vibevoice-server/voice-sample.wav

# Now its own repo (github.com/youngceo-nl/vibevoice-server) - not tracked here
vibevoice-server/

# Local browser profiles contain cookies, login databases, and session state.
worker/loom-session/

# Generated Python bytecode.
__pycache__/
*.pyc

# TypeScript incremental build artifacts
*.tsbuildinfo

# Local isolated implementation worktrees.
.worktrees/

# local scratch: generated migration paste file
apply-run-tables.sql

apply-steel-base-url.sql
apply-cf-access.sql
